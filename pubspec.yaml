name: capyba_challenge
description: Projeto criado para desafio do processo seletivo Capyba 2021.1
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  google_fonts: ^2.0.0
  http: ^0.13.0
  flutter_feather_icons: ^1.0.3
  image_cropper: ^1.3.1
  image_picker: ^0.7.2
  firebase_core: ^1.0.0
  firebase_auth: ^1.0.0
  cloud_firestore: ^1.0.0
  firebase_storage: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
